{
  "name": "objetivos-rekluta",
  "version": "1.10.1",
  "private": true,
  "description": "Dashboard de objetivos e inversion publicitaria de Rekluta",
  "scripts": {
    "dev": "live-server --port=3000 --open=index.html --no-browser",
    "build": "node scripts/build.js"
  },
  "engines": { "node": ">=20" },
  "dependencies": {},
  "devDependencies": { "live-server": "^1.2.2" }
}
